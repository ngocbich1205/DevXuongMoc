@model DevXuongMoc.Areas.CustomerUser.Models.LoginUser
@{
    ViewData["Title"] = "Đăng nhập";
    Layout = "~/Areas/CustomerUser/Views/Shared/_Customer.cshtml";
}
@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger">
        <ul>
            @foreach (var modelState in ViewData.ModelState.Values)
            {
                foreach (var error in modelState.Errors)
                {
                    <li>@error.ErrorMessage</li>
                }
            }
        </ul>
    </div>
}
<body>
    <div class="position-relative vh-100">
        <!-- Background image -->
        <img src="https://media.istockphoto.com/id/1202470984/vi/anh/ch%C3%BAng-t%C3%B4i-c%C3%B3-m%E1%BB%8Di-lo%E1%BA%A1i-g%E1%BB%97-b%E1%BA%A1n-%C4%91ang-t%C3%ACm-ki%E1%BA%BFm.jpg?s=612x612&w=0&k=20&c=yzJG6FH_LJnAcl99Jpv1qppDNwEmxktMCLs0rd4Rf9w="
             alt="Background" class="position-absolute top-0 start-0 w-100 h-100 object-fit-cover">

        <!-- Overlay with lighter opacity -->
        <div class="position-absolute top-0 start-0 w-100 h-100" style="background-color: rgba(0, 0, 0, 0.3); z-index: 1;"></div>

        <!-- Form container -->
        <div class="d-flex justify-content-center align-items-center h-100 position-relative" style="z-index: 2;">
            <section class="bg-white p-5 rounded-4 shadow-lg" style="max-width: 400px; width: 100%; z-index: 2;">
                <h2 class="text-center mb-5 text-dark">Đăng nhập</h2>
                <form asp-action="Index" method="post">
                    <!-- Email input -->
                    <div class="form-outline mb-4">
                        <input asp-for="Email" type="email" id="form3Example3"
                               class="form-control form-control-lg custom-input"
                               placeholder="Email" required />
                    </div>

                    <!-- Password input -->
                    <div class="form-outline mb-4">
                        <input asp-for="Password" type="password" id="form3Example4"
                               class="form-control form-control-lg custom-input"
                               placeholder="Mật khẩu" required />
                    </div>

                    <!-- Forgot password link -->
                    <div class="d-flex justify-content-between align-items-center">
                        <a asp-controller="ForgotPassword" asp-action="ForgotPassword" class="text-body text-decoration-none">Quên mật khẩu?</a>
                    </div>

                    <!-- Submit button and register link -->
                    <div class="text-center text-lg-start mt-4 pt-2">
                        <button type="submit" class="btn btn-primary btn-lg custom-btn" style="width: 100%;">
                            Đăng nhập
                        </button>
                        <p class="small fw-bold mt-2 pt-1 mb-0">
                            Bạn chưa có tài khoản?
                            <a asp-controller="Customers" asp-action="Create" class="link-danger">Đăng ký</a>
                        </p>
                    </div>
                </form>
            </section>
        </div>
    </div>


    <script src="../../plugins/jquery/jquery.min.js"></script>
    <script src="../../plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="../../dist/js/adminlte.min.js"></script>
</body>